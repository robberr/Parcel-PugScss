{
  "name": "parcel-pug-scss",
  "version": "1.0.0",
  "description": "Parcel-PugScss",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.pug --open",
    "build": "parcel build src/index.pug -d docs --public-url ./"
  },
  "author": {
    "name": ""
  },
  "devDependencies": {
    "pug": "^3.0.0",
    "sass": "^1.27.0"
  }
}
